- name: gather facts
  hosts: local
  connection: local
  tasks:
  - name: print facts
    ansible.builtin.debug:
      msg: "The ansible version is {{ ansible_version.full }}"
      msg: "{{ ansible_facts }}"