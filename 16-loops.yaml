- name: demo on loops
  hosts: local
  connection: local
  tasks:
  - name: print names 
    ansible.builtin.debug:
      msg: "{{ item }}"
    loop:
      - "Alice"
      - "Bob"
      - "Charlie"
      - "David"
      - 1431
      - - 3.14159
